<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_image"
    tools:context=".views.MainHomeScreenActivity"
    tools:openDrawer="start">

    <!--  Including all the home view content  -->
    <include
        layout="@layout/home_view_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!--  Navigation View  -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start|top"
        android:background="@color/home_navigation_drawer_bg"
        app:headerLayout="@layout/nav_header_nav"
        app:itemBackground="@android:color/transparent"
        app:itemIconPadding="@dimen/navigation_menu_icon_padding"
        app:itemIconTint="@color/home_navigation_drawer_icon_bg"
        app:itemTextAppearance="@style/custom_menu_font_style"
        app:menu="@menu/activity_nav_drawer">

    </com.google.android.material.navigation.NavigationView>


</androidx.drawerlayout.widget.DrawerLayout>